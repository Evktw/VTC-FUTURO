{% extends 'base.html.twig' %}
{% block body %}
    </br>
    <div class="card">
        <div class="card-body">
            {{ include('form/employe.html.twig') }}
        </div>
    </div>
{% endblock %}
